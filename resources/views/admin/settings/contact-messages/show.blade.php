@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><div><a href="{{ route('admin.settings.contact-messages') }}" class="text-sm font-bold text-slate-500 hover:text-indigo-600"><i class="fa-solid fa-arrow-left mr-2"></i>Back to messages</a><h1 class="mt-5 text-3xl font-black text-slate-900">{{ $contactMessage->subject ?: 'Contact Message' }}</h1><p class="mt-1 text-sm text-slate-500">Received {{ $contactMessage->created_at?->format('d M Y, h:i A') }}</p></div>@if(session('success'))<div class="rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
<div class="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><article class="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><div class="grid gap-4 sm:grid-cols-2"><div><small class="text-xs font-bold uppercase tracking-wider text-slate-400">Name</small><p class="mt-1 font-bold text-slate-800">{{ $contactMessage->name }}</p></div><div><small class="text-xs font-bold uppercase tracking-wider text-slate-400">Email</small><p class="mt-1 break-all font-bold text-slate-800">{{ $contactMessage->email }}</p></div>@if($contactMessage->phone)<div><small class="text-xs font-bold uppercase tracking-wider text-slate-400">Phone</small><p class="mt-1 font-bold text-slate-800">{{ $contactMessage->phone }}</p></div>@endif</div><div class="mt-7 border-t border-slate-100 pt-7"><small class="text-xs font-bold uppercase tracking-wider text-slate-400">Message</small><p class="mt-3 whitespace-pre-line text-sm leading-7 text-slate-700">{{ $contactMessage->message }}</p></div></article><aside class="space-y-5"><form method="POST" action="{{ route('admin.settings.contact-messages.update',$contactMessage) }}" class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @method('PATCH')<h2 class="font-black text-slate-900">Manage message</h2><label class="mt-5 block"><span class="mb-2 block text-sm font-bold">Status</span><select name="status" class="w-full rounded-xl border border-slate-200 px-4 py-3">@foreach(['new','read','replied'] as $status)<option value="{{ $status }}" @selected($contactMessage->status===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><label class="mt-4 block"><span class="mb-2 block text-sm font-bold">Admin note</span><textarea name="admin_note" rows="6" class="w-full rounded-xl border border-slate-200 px-4 py-3">{{ $contactMessage->admin_note }}</textarea></label><button class="mt-4 w-full rounded-xl bg-indigo-600 px-5 py-3 text-sm font-black text-white">Save Message</button></form><form method="POST" action="{{ route('admin.settings.contact-messages.delete',$contactMessage) }}" onsubmit="return confirm('Delete this message permanently?')">@csrf @method('DELETE')<button class="w-full rounded-xl border border-red-200 bg-red-50 px-5 py-3 text-sm font-black text-red-600">Delete Message</button></form></aside></div></div>
@endsection
