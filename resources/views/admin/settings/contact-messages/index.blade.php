@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-7xl space-y-6"><div><p class="text-sm font-black uppercase tracking-widest text-indigo-600">Settings / Contact</p><h1 class="text-3xl font-black text-slate-900">Contact Messages</h1><p class="mt-1 text-sm text-slate-500">Review and manage messages submitted through the website contact form.</p></div>@if(session('success'))<div class="rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
<div class="flex flex-wrap gap-2"><a href="{{ route('admin.settings.contact-messages') }}" class="rounded-xl px-4 py-2 text-sm font-bold {{ !$status?'bg-slate-900 text-white':'bg-white text-slate-600 ring-1 ring-slate-200' }}">All</a>@foreach(['new'=>'New','read'=>'Read','replied'=>'Replied'] as $key=>$label)<a href="{{ route('admin.settings.contact-messages',['status'=>$key]) }}" class="rounded-xl px-4 py-2 text-sm font-bold {{ $status===$key?'bg-slate-900 text-white':'bg-white text-slate-600 ring-1 ring-slate-200' }}">{{ $label }}</a>@endforeach</div>
<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div class="admin-table-wrap"><table class="w-full text-left"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Sender</th><th class="px-5 py-4">Subject</th><th class="px-5 py-4">Message</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Date</th><th class="px-5 py-4 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($messages as $message)<tr class="{{ $message->status==='new'?'bg-indigo-50/30':'' }}"><td class="px-5 py-4"><p class="font-black text-slate-900">{{ $message->name }}</p><p class="text-xs text-slate-500">{{ $message->email }}</p>@if($message->phone)<p class="text-xs text-slate-500">{{ $message->phone }}</p>@endif</td><td class="px-5 py-4 text-sm font-semibold text-slate-700">{{ $message->subject ?: 'No subject' }}</td><td class="max-w-md px-5 py-4 text-sm text-slate-500">{{ \Illuminate\Support\Str::limit($message->message,100) }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $message->status==='new'?'bg-indigo-50 text-indigo-700':($message->status==='replied'?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600') }}">{{ ucfirst($message->status) }}</span></td><td class="px-5 py-4 text-xs text-slate-500">{{ $message->created_at?->format('d M Y, h:i A') }}</td><td class="px-5 py-4 text-right"><a href="{{ route('admin.settings.contact-messages.show',$message) }}" class="inline-flex rounded-lg bg-indigo-50 px-3 py-2 text-xs font-bold text-indigo-600">View</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-sm text-slate-500">No contact messages found.</td></tr>@endforelse</tbody></table></div></div>{{ $messages->links() }}</div>
@endsection
