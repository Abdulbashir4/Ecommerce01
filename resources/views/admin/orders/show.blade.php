@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-6xl space-y-5"><div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p class="text-sm font-bold uppercase tracking-widest text-indigo-600">Order Details</p><h1 class="text-3xl font-black">Order #{{ $order->order_id }}</h1><p class="mt-1 text-sm text-slate-500">{{ $order->created_at?->format('d M Y, h:i A') }}</p></div><a href="{{ url('/admin/orders') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-bold">← Back to orders</a></div>
<div class="grid gap-5 lg:grid-cols-3"><section class="space-y-5 lg:col-span-2"><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><h2 class="font-black">Items</h2><span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold text-indigo-700">{{ $order->order_status }}</span></div><div class="mt-4 divide-y">@foreach($order->items as $item)<div class="flex items-center justify-between gap-4 py-4"><div class="min-w-0"><div class="font-bold">{{ $item->product_name }}</div><div class="text-xs text-slate-500">Qty {{ $item->qty }} · ৳ {{ number_format($item->price,2) }} each</div></div><div class="font-black">৳ {{ number_format($item->total,2) }}</div></div>@endforeach</div><div class="mt-4 border-t pt-4 text-right"><span class="text-sm text-slate-500">Grand Total</span><div class="text-2xl font-black">৳ {{ number_format($order->total_amount,2) }}</div></div></div>
<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-black">Update Order</h2><form method="POST" action="{{ url('/admin/orders/'.$order->order_id) }}" class="mt-4 grid gap-4 sm:grid-cols-3">@csrf @method('PATCH')<select name="order_status" class="rounded-xl border border-slate-200 px-4 py-3">@foreach(['Pending','Processing','Shipped','Completed','Cancelled'] as $s)<option @selected($order->order_status===$s)>{{ $s }}</option>@endforeach</select><select name="payment_status" class="rounded-xl border border-slate-200 px-4 py-3">@foreach(['Unpaid','Paid','Refunded'] as $s)<option @selected($order->payment_status===$s)>{{ $s }}</option>@endforeach</select><button class="rounded-xl bg-gradient-to-r from-indigo-600 to-sky-500 px-5 py-3 font-black text-white">Save Changes</button></form></div></section>
<aside class="space-y-5"><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-black">Customer</h2><div class="mt-4 space-y-3 text-sm"><div><span class="text-slate-500">Name</span><div class="font-bold">{{ $order->customer_name }}</div></div><div><span class="text-slate-500">Phone</span><div class="font-bold">{{ $order->phone }}</div></div><div><span class="text-slate-500">Email</span><div class="font-bold">{{ $order->email ?: '—' }}</div></div><div><span class="text-slate-500">Address</span><div class="font-bold">{{ $order->address }}, {{ $order->city }} {{ $order->postal_code }}</div></div><div><span class="text-slate-500">Country</span><div class="font-bold">{{ $order->country }}</div></div><div><span class="text-slate-500">Payment method</span><div class="font-bold">{{ $order->payment_method }}</div></div></div></div></aside></div></div>
@endsection
