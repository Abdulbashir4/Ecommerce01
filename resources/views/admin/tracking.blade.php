@extends('layouts.admin')
@section('content')<div class="mx-auto max-w-7xl space-y-5"><div><p class="text-sm font-bold uppercase tracking-widest text-indigo-600">Orders</p><h1 class="text-3xl font-black">Order Tracking</h1><p class="mt-1 text-sm text-slate-500">Track each order using its current status.</p></div><form class="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div class="flex gap-3"><input name="q" value="{{ request('q') }}" placeholder="Enter Order ID" class="min-w-0 flex-1 rounded-xl border border-slate-200 px-4 py-3"><button class="rounded-xl bg-slate-950 px-5 py-3 font-black text-white">Track</button></div></form><div class="grid gap-4">@foreach($orders as $o)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><div class="text-lg font-black">Order #{{ $o->order_id }}</div><div class="mt-1 text-sm text-slate-500">{{ $o->customer_name }} · {{ $o->phone }}</div></div><div class="text-right"><div class="text-lg font-black">৳ {{ number_format($o->total_amount,2) }}</div><div class="text-xs font-bold text-slate-500">{{ $o->created_at?->format('d M Y, h:i A') }}</div></div></div><div class="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">@foreach(['Pending','Processing','Shipped','Completed','Cancelled'] as $step)<div class="text-center"><div class="mx-auto grid h-9 w-9 place-items-center rounded-full {{ $o->order_status===$step?'bg-indigo-600 text-white':($step==='Cancelled'&&$o->order_status==='Cancelled'?'bg-red-600 text-white':'bg-slate-100 text-slate-400') }}">@if($step==='Pending')<i class="fa-regular fa-clock text-xs"></i>@elseif($step==='Processing')<i class="fa-solid fa-gear text-xs"></i>@elseif($step==='Shipped')<i class="fa-solid fa-truck text-xs"></i>@elseif($step==='Completed')<i class="fa-solid fa-check text-xs"></i>@else<i class="fa-solid fa-xmark text-xs"></i>@endif</div><div class="mt-2 text-[10px] font-bold text-slate-500">{{ $step }}</div></div>@endforeach</div></div>@endforeach</div>{{ $orders->links() }}</div>@endsection
