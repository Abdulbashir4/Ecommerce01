@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-7xl space-y-5"><div><p class="text-sm font-black uppercase tracking-widest text-indigo-600">Security</p><h1 class="text-3xl font-black">Audit Log</h1><p class="mt-1 text-sm text-slate-500">Who changed what, when and from which IP.</p></div><form class="flex gap-3 rounded-2xl border bg-white p-4"><input name="q" value="{{ request('q') }}" placeholder="Search action..." class="flex-1 rounded-xl border px-4 py-3"><button class="rounded-xl bg-slate-950 px-5 font-black text-white">Search</button></form><div class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Time</th><th class="p-4">Actor</th><th class="p-4">Action</th><th class="p-4">Target</th><th class="p-4">IP</th></tr></thead><tbody class="divide-y">@forelse($logs as $log)<tr><td class="p-4 text-xs">{{ $log->created_at->format('d M Y h:i A') }}</td><td class="p-4 font-bold">{{ $log->actor?->name ?? 'System' }}</td><td class="p-4"><span class="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-bold text-indigo-700">{{ $log->action }}</span></td><td class="p-4">{{ $log->target?->name ?? ($log->metadata['deleted_user_name'] ?? '—') }}</td><td class="p-4 text-xs text-slate-500">{{ $log->ip_address ?? '—' }}</td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-slate-500">No audit records.</td></tr>@endforelse</tbody></table></div></div>{{ $logs->links() }}</div>
@endsection
