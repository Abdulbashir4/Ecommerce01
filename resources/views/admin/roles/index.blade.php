@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-7xl space-y-5">
 <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p class="text-sm font-black uppercase tracking-widest text-indigo-600">Security</p><h1 class="text-3xl font-black">Roles & Permissions</h1><p class="mt-1 text-sm text-slate-500">Create roles and decide exactly what each role can control.</p></div><a href="{{ route('admin.roles.create') }}" class="rounded-xl bg-slate-950 px-5 py-3 text-sm font-black text-white"><i class="fa-solid fa-plus mr-2"></i>Create Role</a></div>
 <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@foreach($roles as $role)<article class="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div><h2 class="text-xl font-black">{{ $role->name }}</h2><p class="mt-1 text-xs text-slate-500">{{ $role->slug }}</p></div><span class="rounded-full px-3 py-1 text-xs font-black {{ $role->is_system?'bg-indigo-50 text-indigo-700':'bg-slate-100 text-slate-700' }}">{{ $role->is_system?'System':'Custom' }}</span></div><p class="mt-3 text-sm text-slate-500">{{ $role->description }}</p><div class="mt-4 flex items-center justify-between text-sm"><span><strong>{{ $role->users_count }}</strong> users</span><span><strong>{{ $role->permissions->count() }}</strong> permissions</span></div><div class="mt-5 flex gap-2"><a href="{{ route('admin.roles.edit',$role) }}" class="flex-1 rounded-xl border px-4 py-2.5 text-center text-sm font-black text-indigo-600">Manage</a>@if(!$role->is_system)<form method="POST" action="{{ route('admin.roles.delete',$role) }}" onsubmit="return confirm('Delete this role?')">@csrf @method('DELETE')<button class="rounded-xl border border-red-100 px-4 py-2.5 text-sm font-black text-red-600">Delete</button></form>@endif</div></article>@endforeach</div>
</div>
@endsection
