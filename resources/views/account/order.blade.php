@extends('layouts.app') @section('content')<div class="flex items-end justify-between"><div><p class="text-sm text-slate-500">Order</p><h1 class="text-4xl font-black">#{{ $order->order_id }}</h1></div><div class="text-right"><p>{{ $order->order_status }}</p><p>{{ $order->payment_status }}</p></div></div><div class="mt-8 rounded-2xl border bg-white">@foreach($order->items as $i)<div class="flex justify-between border-b p-5"><span>{{ $i->product_name }} × {{ $i->qty }}</span><span>৳ {{ number_format($i->total,2) }}</span></div>@endforeach<div class="flex justify-between p-5 text-xl font-black"><span>Total</span><span>৳ {{ number_format($order->total_amount,2) }}</span></div></div>@endsection
